% Geometry inputs
function [M,N,L,t0] = Geometry(Dim)
    %Geometry 
    M = Dim; % Number of points in x direction
    N = Dim; % Number of points in y direction
    L = 1; % [m] - Length of the sides
    t0 = 1; % [s];

end